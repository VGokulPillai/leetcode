class Solution:
    def canBeEqual(self, target: List[int], arr: List[int]) -> bool:
        arr=sorted(arr)
        count=0
        for i in range(len(target)):
            if(target[i]==arr[i]):
                count+=1
        if(count==len(arr)):
            return True
        else:
            return False
        
